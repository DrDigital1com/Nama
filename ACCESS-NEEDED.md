# מה נדרש כדי להשלים את הבדיקה החיה

הסביבה שבה אני פועל **חוסמת גישת רשת ל-`nama-c.com`** (מדיניות egress — התקבל 403 על CONNECT, וגם fetch מנוהל החזיר `EGRESS_BLOCKED`), והריפו `DrDigital1com/Nama` הגיע ריק לחלוטין — אפס קומיטים, אין קוד תבנית או תוספים.

לכן הבדיקה עצמה חייבת לרוץ מהצד שלך. להלן שלוש דרכים, לפי רמת הגישה.

---

## מסלול א׳ — ללא SSH (הכי פשוט, 10 דקות)

1. להעלות את `tools/nama-audit.php` ל-`wp-content/mu-plugins/`
   (אם התיקייה לא קיימת — ליצור אותה).
2. **כלים → Nama Audit → הרצת בדיקה מלאה**.
3. לגלול לתחתית, להעתיק את כל תיבת ה-JSON, ולשלוח.

ה-JSON **אינו כולל** שמות לקוחות, מיילים, כתובות, טלפונים או פרטי תשלום — רק נתונים מצטברים ומזהי הזמנות.

---

## מסלול ב׳ — עם SSH (מקיף יותר)

```bash
cd /path/to/wordpress
bash nama-cli-audit.sh > nama-report.txt 2>&1
```
ולשלוח את `nama-report.txt`.

---

## מסלול ג׳ — SQL בלבד (phpMyAdmin)

להריץ את `sql/orders-diagnostics.sql` בלוק-בלוק ולשלוח את הפלט.
**המינימום ההכרחי:** בדיקות A1, A3, A5, **A7**, C1, C3.

בדיקה **A7** היא החשובה ביותר — היא מגלה אם יש לקוחות ששילמו וההזמנה שלהם לא עודכנה.

---

## בנוסף — אם אפשר

| פריט | למה זה נדרש |
|---|---|
| קישור PageSpeed Insights לדף הבית **ולעמוד מוצר** | Core Web Vitals אמיתיים משדה — אי אפשר להסיק אותם מהשרת |
| צילום מסך: `WooCommerce → סטטוס` | דוח הבריאות הרשמי של ווקומרס |
| צילום מסך: `WooCommerce → סטטוס → Scheduled Actions` | מצב תור הפעולות |
| שם שער הסליקה בשימוש | תוספי הסליקה הישראליים נבדלים מהותית בהתנהגות ה-callback |
| 3–5 מספרי הזמנות שנתקעו | לניתוח נקודתי מול היומנים |
| האם יש דוח עסקאות מחברת הסליקה | הדרך היחידה לאמת אם הלקוחות שילמו בפועל |

---

## אם רוצים שאבצע את הבדיקה ישירות

שתי אפשרויות:

1. **לאשר את `nama-c.com` במדיניות ה-egress** של סביבת ההרצה — ואז אוכל למדוד את האתר החי בעצמי (HTML, זמני תגובה, כותרות מטמון, נכסים).
2. **להעלות את קוד האתר לריפו** (תבנית + רשימת תוספים, ללא `wp-config.php` וללא סודות) — ואז אוכל לבצע סקירת קוד ממשית של התבנית ושל התאמות הצ׳קאאוט.

> **אזהרת אבטחה:** לא לשלוח סיסמאות, מפתחות API של שער הסליקה, או תוכן `wp-config.php` בערוץ הזה.
